namespace

procedure
structure
unistruct
enumerare
datum

compare
case
jumpto
return

namespace {
    unistruct
    structure
    datum
    procedure {
        label
        datum
        compare {
            case {
                jumpto
                return
            }
        }
        jumpto
        call
        assign
        return
    }
}

procedure Compare(first :: U8, second :: U8) {
    datum result :: U8;
    compare first, second {
        case >= {
            jumpto GE;
        },
        case < {
            jumpto LT;
        },
    }
    return first == second;
label GE :=
    Console:Write("");
    jumpto END;
label LT :=
    Console:Write("");
label END :=
}
