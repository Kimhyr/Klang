
#include <iostream>

#include "Core.hpp"

/*

Lexer lexer("procedure");
KeywordT keyword;
Token::Flag flags = keyword.Lex(&lexer);
if (Token::HasError(flags)) {
  switch (this->errBuf->Last()) {
    // Handle error
  }
}
std::cout << (UInt8)keyword.value;
*/

Int32 Initiate() {
  std::cout << "Hello, World!\n";
  return 0;
}
